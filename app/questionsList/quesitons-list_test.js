'use strict';

describe('app.questionsList module', function() {

  beforeEach(module('app.questionsList'));

  describe('questionsList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var questionsListCtrl = $controller('QuestionsListCtrl');
      expect(questionsListCtrl).toBeDefined();
    }));

  });
});